// Multilang support
import en from '../../translations/en.json';
import fa from '../../translations/fa.json';

export const resources = {
  en: {translation: en},
  fa: {translation: fa},
};

export const Config = {
  OTPLength: 5,
  CountDownTimer: 120,
};
